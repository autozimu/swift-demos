matrix:
  include:
    - os: linux
      dist: trusty
      sudo: required
    - os: osx
      osx_image: xcode8
install:
    - . ./.travis-install-swift.sh
script:
  - cd ./source-layout-sample && swift build && swift test
